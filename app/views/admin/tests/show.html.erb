<h1><p>
  <strong>Title:</strong>
  <%= @test.title %>
</p>
<p>
  <strong>Level:</strong>
  <%= @test.level %>
</p></h1>

<h3> Question: </h3>

<%= link_to 'New question', new_admin_test_question_path(@test) %>

<ol>
  <%= render @test.questions %>
</ol>

<p><%= link_to 'Edit', edit_admin_test_path(@test) %>
   <%= link_to 'Back', admin_tests_path %>
   <%= button_to 'Delete', admin_test_path(@test), method: :delete, data: {confirm: 'Are you sure?'}%>
</p>
