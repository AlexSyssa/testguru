<tr>
  <td><%= badge.title %></td>
  <td><%= badge.rule %></td>
  <td><%= image_tag "#{badge.image}", {height: 45}%></td>
  <td><%= link_to t('.show'), admin_badge_path(badge) %></td>
  <td><%= link_to t('.edit'), edit_admin_badge_path(badge) %></td>
  <td><%= button_to 'delete', admin_badge_path(badge), method: :delete %></td>
</tr>
