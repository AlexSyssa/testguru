<nav class="navbar fixed-top bg-body-tertiary" style="background-color: #AFEEEE;">
  <div class="container-fluid">
    <%= link_to t('.testguru'), root_path, class: 'navbar-brand' %>

    <span class="nav justify-content-end">
      <% if user_signed_in? %>
        <%= t('.welcome', name: current_user.first_name) %> |
        <%= button_to t('.logout'), destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?'} %>
      <% else %>
        <%= link_to "Log in", new_session_path(resource_name) %> |
        <%= link_to "Sign up", new_registration_path(resource_name) %>
      <% end %>
    </span>
  </div>
</nav>
