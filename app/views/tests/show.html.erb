<h1><p>
  <strong>Title:</strong>
  <%= @test.title %>
</p>
<p>
  <strong>Level:</strong>
  <%= @test.level %>
</p></h1>

<h3> Question: </h3>

<%= link_to 'New question', new_test_question_path(@test) %>

<ol>
  <%= render @test.questions %>
</ol>
<p><%= link_to 'Edit', edit_test_path(@test) %>
   <%= link_to 'Back', tests_path %>
   <%= link_to fa_icon('trash-o'), test_path(@test), method: :delete, title: 'Delete test' %>
</p>
